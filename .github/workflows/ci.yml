name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

# OSSF Scorecard TokenPermissions: top-level read-all so the default token
# is least-privilege. Jobs may grant narrower per-job scopes if needed.
permissions: read-all

env:
  DOTNET_NOLOGO: "true"
  DOTNET_CLI_TELEMETRY_OPTOUT: "true"
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: "true"

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup .NET 9 and 10
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5.4.0
        with:
          # [issue #109] The plugin multi-targets net9.0 (Jellyfin 10.11)
          # and net10.0 (Jellyfin 12). Building needs only the .NET 10 SDK,
          # but running the net9.0 test suite needs the .NET 9 runtime too.
          dotnet-version: |
            9.0.x
            10.0.x

      - name: Cache NuGet packages
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore
        run: |
          dotnet restore Jellyfin.Plugin.AchievementBadges/Jellyfin.Plugin.AchievementBadges.csproj --locked-mode
          dotnet restore Jellyfin.Plugin.AchievementBadges.Tests/Jellyfin.Plugin.AchievementBadges.Tests.csproj --locked-mode

      - name: Build
        run: |
          dotnet build Jellyfin.Plugin.AchievementBadges/Jellyfin.Plugin.AchievementBadges.csproj --configuration Release --no-restore
          dotnet build Jellyfin.Plugin.AchievementBadges.Tests/Jellyfin.Plugin.AchievementBadges.Tests.csproj --configuration Release --no-restore

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup .NET 9 and 10
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5.4.0
        with:
          # [issue #109] The plugin multi-targets net9.0 (Jellyfin 10.11)
          # and net10.0 (Jellyfin 12). Building needs only the .NET 10 SDK,
          # but running the net9.0 test suite needs the .NET 9 runtime too.
          dotnet-version: |
            9.0.x
            10.0.x

      - name: Cache NuGet packages
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore
        run: dotnet restore Jellyfin.Plugin.AchievementBadges.Tests/Jellyfin.Plugin.AchievementBadges.Tests.csproj --locked-mode

      - name: Test
        run: |
          dotnet test Jellyfin.Plugin.AchievementBadges.Tests/Jellyfin.Plugin.AchievementBadges.Tests.csproj \
            --configuration Release \
            --no-restore \
            --logger "trx;LogFileName=test-results.trx" \
            --results-directory ./TestResults

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-results
          path: ./TestResults
